import { } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 8,
        backgroundColor: 'coral',
    },
    esconder: {
        backgroundColor: '#f00',
        heigth: 10,
        width: '100%',
    },
});

export default styles